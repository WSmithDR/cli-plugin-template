# Completados — cli-plugin-template

_Última actualización: 2026-08-24_

- [x] **CATALOG-GUARD bloquea `src/features/` de apps que no son plugins** — el regex `(^|/)features/([^/]+)/` matchea cualquier segmento del path, así que en un repo de aplicación con `src/features/<modulo>/` el hook bloquea Write/Edit/Read exigiendo un `meta.yml` que ahí no corresponde _(creado por: SmithDR · 2026-08-24)_ ✓ _resuelto: la regla 1 ahora exige el sentinel `.catalog-root` en algún ancestro del feature (mismo criterio que `session-start.sh`, `intent-nudge.py` y `paths.ts`); la regla 2 (scripts embebidos en SKILL.md) sigue aplicando a cualquier plugin. Espejado en `.opencode/lib/catalog-guard.ts`; los fixtures de `test-opencode-shim.sh` que dependían del bug ahora arman su propio subárbol con sentinel — responsable: SmithDR · 2026-08-24T21:21-05:00_
- [x] **El audit de portabilidad escanea archivos gitignoreados** — `.claude/settings.local.json`, que Claude Code auto-edita con rutas absolutas al aprobar comandos, generaba CRITICAL y frenaba el pre-commit pese a no viajar nunca en el commit _(creado por: SmithDR · 2026-08-24)_ ✓ _resuelto: `walk()` filtra lo que git ignora con una sola invocación de `git check-ignore -z --stdin` (fallback silencioso sin git o fuera de working tree; los trackeados se siguen auditando aunque matcheen un patrón). `.portabilityignore` queda para exclusiones adicionales. Feature bumpeado a 1.0.1 — responsable: SmithDR · 2026-08-24T21:21-05:00_
- [x] **El Stop hook no detecta cómo se queja el usuario de verdad** — las `FRICTION_KEYWORDS` eran literales ("no funciona", "está mal"), así que una sesión entera con cuatro correcciones seguidas («le falta coherencia», «corrígelo también», «rehazlo completo mejor») no propuso cosechar nada _(creado por: SmithDR · 2026-08-24)_ ✓ _resuelto: heurística regex sobre texto normalizado (minúsculas sin tildes) que cubre imperativos de re-trabajo y juicios blandos, deliberadamente sin infinitivos para no matchear prosa de trabajo normal. Además el texto del usuario se poda de tool results, entradas meta y system-reminders, que viajan como `type=user` y harían disparar el hook en cualquier sesión. 5 casos nuevos, 15/15 pasan — responsable: SmithDR · 2026-08-24T21:21-05:00_
- [x] **El Step 6 pide permiso antes de registrar una fricción** — preguntar por cada hallazgo convierte al usuario en cuello de botella de su propia sesión, y los hallazgos se pierden cuando no está mirando _(creado por: SmithDR · 2026-08-24)_ ✓ _resuelto: `plugin-feedback-log` Step 6 registra en el acto y avisa en una línea (`slug` + `signal`); la pregunta queda solo para el caso de atribución dudosa. Principio escrito en la skill: se pide permiso para cambiar código, no para tomar nota — el gate ya está en `plugin-hotpatch` — responsable: SmithDR · 2026-08-24T21:21-05:00_
- [x] **No se puede dar de alta un plugin sin estar parado en su repo** — la fricción con un plugin aparece usándolo en otro proyecto, que es justo donde `plugin-register` no servía; había que abandonar la tarea, ir al repo y volver _(creado por: SmithDR · 2026-08-24)_ ✓ _resuelto: `gather-plugin-metadata.sh` acepta la ruta por argumento (sin argumento, cwd como antes) y rechaza rutas del cache de plugins, que no sirven como `local_path` porque `plugin-hotpatch` parchearía la copia instalada. `plugin-register` Step 1 resuelve en cascada ruta → nombre (`installed_plugins.json`) → cwd → preguntar, y `plugin-feedback-log` ofrece el alta en el acto cuando el resolve falla. Implementado como argumento de skill y no como `commands/`, para no chocar con los comandos nativos. 5 tests nuevos, 10/10 pasan — responsable: SmithDR · 2026-08-24T21:21-05:00_

- [x] **Un feedback capturado con `--plugin-dir` no dice desde qué árbol corrió el plugin** — el sello de versión sale del manifiesto en `local_path`, así que probando un plugin local desde un worktree, otro clon o `claude --plugin-dir <ruta>`, el feedback queda atribuido al repo del registry y `plugin-hotpatch` parchea un árbol distinto del que produjo la fricción, sin avisar _(creado por: SmithDR · 2026-08-22)_ ✓ _resuelto: campo `plugin_path` en el frontmatter, sellado por `_foreign_path()` en `bin/lib/gateway.py`. Solo lo declara quien captura (`cpt` no puede inferirlo: su `CLAUDE_PLUGIN_ROOT` apunta al meta-plugin), se descarta si coincide con `local_path`, se preserva al re-guardar y se conserva aunque la ruta ya no exista. Documentado en la tabla del Step 2 y el template de `plugin-feedback-log`, junto con la corrección de de dónde sale `plugin_version` (la base directory de la skill, nunca el listado de `plugins/cache`). 2 tests nuevos, 16/16 pasan — responsable: SmithDR · 2026-08-22T18:43-05:00_

- [x] **Los feedbacks no dicen a qué versión del plugin se refieren** — un feedback guardado parece hablar del código de hoy: nada en el frontmatter ata la observación a la versión en la que se hizo, así que un `patch_target` puede haber dejado de existir entre la captura y el patch sin que nada lo señale _(creado por: SmithDR · 2026-08-22)_ ✓ _resuelto: el store sella `plugin_version` como sella `created` — gana el que declara quien captura, después el del archivo previo, y solo si el archivo es nuevo sale del manifiesto en `local_path`; a un feedback viejo sin sello no se le inventa uno. Detectado en vivo: ankify pasó de 1.88.8 a 2.0.0 (una reescritura) entre la captura y el patch, y 9 de 10 `patch_target` quedaron apuntando a archivos muertos. Tocó `bin/lib/gateway.py`, `bin/test-cpt-feedback.sh` (2 casos nuevos, 14/14 pasan), `skills/plugin-feedback-log/SKILL.md` y su `references/feedback-template.md` — responsable: SmithDR · 2026-08-22T18:21-05:00_
- [x] **Suites de test fuera del CI** — `.github/workflows/validate.yml` corre solo 5 de las 13 suites de `bin/test-*.sh`: quedan afuera `test-detect-feedback.sh`, `test-cpt-registry/feedback/proposal/status/crossrepo.sh`, `test-opencode-shim.sh` y `test-skill-structure.sh`. Hoy esas solo se ejecutan en el hook `pre-commit` local, así que un commit hecho desde la web de GitHub (o con `--no-verify`) puede romper el recolector de feedback, el gateway `cpt` o el shim de OpenCode y el CI igual pasa verde._(creado por: SmithDR · 2026-08-12)_ ✓ _resuelto: validate.yml ahora itera TODO bin/test-*.sh en un solo paso — las 13 suites (incluida test-opencode-shim.sh con runner bun→node strip-types) corren en CI y cualquier suite nueva entra sola — responsable: SmithDR · 2026-08-23T17:37-05:00_

- [x] **Hooks CC del ciclo dual-CLI** — el plan `docs/plans/2026-08-23-dual-cli-hooks.md` nunca fue tarea: faltaban el guard del contrato del catálogo en PreToolUse (meta.yml por walk-de-parents + fence rule de SKILL.md con soporte new_string), el nudge de fricción ante fallos de suite (rekeyeado a `PostToolUseFailure`, evento oficial para tools que fallan, verificado contra doc oficial), el nudge del router plugin-dev en UserPromptSubmit (registry + sentinel `.catalog-root`) y el snapshot WIP best-effort en PreCompact, más una suite compartida que los cubra _(creado por: SmithDR · 2026-08-23)_ ✓ _resuelto: SDD tasks 1–4 con implementador+revisor por tarea; suite bin/test-catalog-hooks.sh 16 casos verdes; commits 9757929..d1d182d — responsable: SmithDR · 2026-08-23T19:46-05:00_

- [x] **Paridad OpenCode de los hooks nuevos** — los hooks CC no tenían espejo TS: sin guard ni nudge al desarrollar plugins desde OpenCode _(creado por: SmithDR · 2026-08-23)_ ✓ _resuelto: tool-guard (before/after) + bootstrap-inject/intent con walk-de-parents replicado, fallback new_string y triggers normalizados gated por registeredCwd; shim extendido a 8+5 casos y tsc limpio — commits 0caa096, 869fab3, 7e07b5d — responsable: SmithDR · 2026-08-23T19:46-05:00_

- [x] **Docs de paridad desactualizadas** — AGENTS.md/CLAUDE.md y tool-mapping.md no mencionaban los 4 hooks nuevos ni el evento correcto (`PostToolUseFailure`, no `PostToolUse`) _(creado por: SmithDR · 2026-08-23)_ ✓ _resuelto: sección Hooks activos + filas en skills/plugin-dev/references/tool-mapping.md contrastadas contra code.claude.com/docs/en/hooks — commits 870841e, c5080e6 — responsable: SmithDR · 2026-08-23T19:46-05:00_

- [x] **Defectos que solo el review holístico de rama veía** — el nudge OC hablaba ante corridas verdes ("0 failed" matcheaba /fail/i) y la suite exigía branch "main" literal en el snapshot WIP _(creado por: SmithDR · 2026-08-23)_ ✓ _resuelto: regex /[1-9]\d* +fail/i con conteo >0 más 2 casos shim nuevos (verde→silencio, rojo→nudge); test verifica branch no-vacío; triage completo de deuda menor en .git/sdd/progress.md — commit 7e07b5d — responsable: SmithDR · 2026-08-23T19:46-05:00_

- [x] **Guard ciego ante MultiEdit** — bin/hooks/catalog-guard.py:48 solo escanea `content`/`new_string` plano, así que un MultiEdit (edits[]) puede inyectar un fenced script >2 líneas en SKILL.md sin ser bloqueado en el momento; hoy lo agarra recién el pre-commit audit-skill-structure.py _(creado por: SmithDR · 2026-08-23)_ ✓ _resuelto: content/new_string/edits[].new_string se escanean por separado (evita falsos fences entre edits) tanto en bin/hooks/catalog-guard.py como en la paridad TS (.opencode/hooks/tool-guard/index.ts beforeHook); caso nuevo en test-catalog-hooks.sh y assert de bloqueo vía edits[] en test-opencode-shim.sh — responsable: SmithDR · 2026-08-23T20:15-05:00_

- [x] **SUITE_RE de OpenCode ignora npm test** — .opencode/hooks/tool-guard/index.ts:17 no incluye `\bnpm (run )?test\b` como la versión Python (bin/hooks/test-failure-nudge.py:10), entonces un `npm test` fallido en OC no dispara el nudge de fricción y ese feedback se pierde silenciosamente _(creado por: SmithDR · 2026-08-23)_ ✓ _resuelto: regex alineada con la Python más caso nuevo "npm run test con fallos → nudge"; suites verdes (shim externo 8 + propio 7) — responsable: SmithDR · 2026-08-23T20:15-05:00_

- [x] **tool-mapping.md sin fila PostToolUseFailure** — la tabla de eventos de skills/plugin-dev/references/tool-mapping.md documenta PreToolUse/PostToolUse/UserPromptSubmit/Stop/PreCompact pero omite PostToolUseFailure, así que un agente portando hooks no sabe que CC distingue fallo (input.error con exit code) y OC no (tool.execute.after corre siempre) _(creado por: SmithDR · 2026-08-23)_ ✓ _resuelto: fila nueva tras PostToolUse con el formato exacto de `input.error`, los casos que NO disparan el evento y la estrategia OC de inspección de output — responsable: SmithDR · 2026-08-23T20:15-05:00_

- [x] **intent-nudge colisión de prefijo** — `startswith(local_path)` hereda el nudge de intención a dirs hermanos cuyo nombre extiende el path registrado (`/tmp/proj-ankify2` matchea `/tmp/proj-ankify`) _(creado por: SmithDR · 2026-08-23)_ ✓ _resuelto: comparación por segmento (`cwd == lp or cwd.startswith(lp + "/")`) en paridad py (bin/hooks/intent-nudge.py) y TS (.opencode/hooks/bootstrap-inject/intent.ts, con normalización de slash final); casos nuevos en ambas suites (subdirectorio legítimo → nudge, hermano con prefijo → silencio); verdes 19 catálogo + 10+7 shim + tsc limpio — responsable: SmithDR · 2026-08-23T20:32-0500_

- [x] **Paridad OpenCode del snapshot WIP (PreCompact)** — el snapshot WIP quedó CC-only porque la API de OC solo exponía `experimental.session.compacting` (customización de prompt), pero hoy existe `session.compacted` _(creado por: SmithDR · 2026-08-23)_ ✓ _resuelto: handler event nuevo .opencode/hooks/compact-event/ que espawnea bin/hooks/wip-snapshot.py (misma fuente que CC, best-effort) filtrando type === "session.compacted", cableado en el entry junto al Stop; casos shim nuevos (session.updated → sin snapshot, session.compacted → snapshot con Branch); fila PreCompact de tool-mapping.md y AGENTS.md actualizadas — verdes 19 catálogo + 10+9 shim + tsc limpio — responsable: SmithDR · 2026-08-23T20:50-0500_
- [x] **Prevención de drift de APIs externas** — convención "afirmación negativa con fecha de verificación" + chequeo automático _(creado por: SmithDR · 2026-08-23)_ ✓ _resuelto: bin/audit-doc-drift.py (marcadores no expone/no emite/sin equivalente fuera de fences y backticks, exige "(verificado YYYY-MM-DD" fresco ≤180d en ±1 línea; excluye docs/plans, .todo, test-* y features/*/files por diseño; report-only exit 0, --json), Parte D en skills/plugin-audit/SKILL.md con protocolo de verificación, claim vivo de stop-hook.ts fechado (verificado 2026-08-23 contra opencode.ai/docs/plugins); multi-cli-compat README resultó ejemplo en fence y tool-mapping PreCompact ya corregido en v1.32.0; suite propia 6 passed + 19 catálogo + shim + tsc limpio — commit b081aa2 — responsable: SmithDR · 2026-08-23T21:11-05:00_
