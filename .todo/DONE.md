# Completados — cli-plugin-template

_Última actualización: 2026-08-23_

- [x] **Un feedback capturado con `--plugin-dir` no dice desde qué árbol corrió el plugin** — el sello de versión sale del manifiesto en `local_path`, así que probando un plugin local desde un worktree, otro clon o `claude --plugin-dir <ruta>`, el feedback queda atribuido al repo del registry y `plugin-hotpatch` parchea un árbol distinto del que produjo la fricción, sin avisar _(creado por: SmithDR · 2026-08-22)_ ✓ _resuelto: campo `plugin_path` en el frontmatter, sellado por `_foreign_path()` en `bin/lib/gateway.py`. Solo lo declara quien captura (`cpt` no puede inferirlo: su `CLAUDE_PLUGIN_ROOT` apunta al meta-plugin), se descarta si coincide con `local_path`, se preserva al re-guardar y se conserva aunque la ruta ya no exista. Documentado en la tabla del Step 2 y el template de `plugin-feedback-log`, junto con la corrección de de dónde sale `plugin_version` (la base directory de la skill, nunca el listado de `plugins/cache`). 2 tests nuevos, 16/16 pasan — responsable: SmithDR · 2026-08-22T18:43-05:00_

- [x] **Los feedbacks no dicen a qué versión del plugin se refieren** — un feedback guardado parece hablar del código de hoy: nada en el frontmatter ata la observación a la versión en la que se hizo, así que un `patch_target` puede haber dejado de existir entre la captura y el patch sin que nada lo señale _(creado por: SmithDR · 2026-08-22)_ ✓ _resuelto: el store sella `plugin_version` como sella `created` — gana el que declara quien captura, después el del archivo previo, y solo si el archivo es nuevo sale del manifiesto en `local_path`; a un feedback viejo sin sello no se le inventa uno. Detectado en vivo: ankify pasó de 1.88.8 a 2.0.0 (una reescritura) entre la captura y el patch, y 9 de 10 `patch_target` quedaron apuntando a archivos muertos. Tocó `bin/lib/gateway.py`, `bin/test-cpt-feedback.sh` (2 casos nuevos, 14/14 pasan), `skills/plugin-feedback-log/SKILL.md` y su `references/feedback-template.md` — responsable: SmithDR · 2026-08-22T18:21-05:00_
- [ ] **Suites de test fuera del CI** — `.github/workflows/validate.yml` corre solo 5 de las 13 suites de `bin/test-*.sh`: quedan afuera `test-detect-feedback.sh`, `test-cpt-registry/feedback/proposal/status/crossrepo.sh`, `test-opencode-shim.sh` y `test-skill-structure.sh`. Hoy esas solo se ejecutan en el hook `pre-commit` local, así que un commit hecho desde la web de GitHub (o con `--no-verify`) puede romper el recolector de feedback, el gateway `cpt` o el shim de OpenCode y el CI igual pasa verde._(creado por: SmithDR · 2026-08-12)_ ✓ _resuelto: validate.yml ahora itera TODO bin/test-*.sh en un solo paso — las 13 suites (incluida test-opencode-shim.sh con runner bun→node strip-types) corren en CI y cualquier suite nueva entra sola — responsable: SmithDR · 2026-08-23T17:37-05:00_
