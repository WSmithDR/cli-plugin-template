name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  hooks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Run hook tests
        run: bash bin/dev/test-hooks.sh
